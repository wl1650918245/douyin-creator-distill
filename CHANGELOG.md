# 更新日志

所有重要变更记录在此。版本遵循 [Semantic Versioning](https://semver.org/)。

## [Unreleased]

### Added

- 内容工作台新增可运行的智能筛选：本地向量索引、自然语言召回、相关度排序、增量复用和显式精排复核；索引状态进入任务中心。
- SQLite 新增 `semantic_documents` 与 `semantic_embeddings`，以 `source_key + video_id` 保证作品身份，以内容哈希判断是否重算。
- 项目独立 Python 环境补齐 CPU `torch`、`transformers` 与 `sentence-transformers`，轻量和高精度模型均完成离线推理回归。
- 设置中心新增智能筛选模型管理：轻量与高精度模式可独立检测、按需下载、删除和自由切换；精排复核复用当前分析模型配置且不向页面暴露密钥。
- 智能筛选模型默认改用 ModelScope 国内源下载，绕开 Hugging Face 在国内网络和 Windows 长路径下的不稳定传输。
- 已通过 ModelScope 国内源实装轻量与高精度模型，并按镜像官方文件大小校验完整性；默认启用轻量模式。
- 文本提取状态卡展示当前作品标题、作品 ID 和实际执行通道；关注页的最新目录、目录版本、完成转写三个统计卡升级为可访问的操作入口。
- 增加按关注来源打开转写资产目录的安全接口；云端与本地 Whisper 分目录保存时最多打开两个通道根目录。
- 统一作品状态总账：按 `source_key + video_id` 去重，关联抓取版本、单作品处理状态、本地产物和任务检查点；历史成功不会被后续失败降级。
- 作品总账增加幂等历史回填和独立回归测试；新转写批次自动跳过已完成及正在处理的作品，并允许历史失败作品重新进入队列。
- 关注博主追加式增量：自动检查加载审核基线，只抓近期作品，并在稳定进入历史重叠区后停止。
- CreatorDistill 公开品牌、产品封面、流程图和能力地图。
- Apache-2.0 许可证、安全政策、贡献指南和 GitHub Issue 模板。
- Windows CI：语法检查与无账号单元测试。
- 全量转写批处理编排：单项失败隔离、有限自动重试、安全暂停、断点继续和失败项重试。
- 转写任务与单作品尝试次数持久化到 SQLite，服务重启后自动恢复未完成批次。
- 不限总数的转写优先级策略：云端优先在额度耗尽后续接 Whisper；Whisper 优先在图文或本地失败后续接云端。

### Fixed

- 修复账号登录验证成功后浏览器继续占用专用 Chrome Profile，导致后续收藏夹抓取连续失败的问题；登录助手现在等待 Cookie 写盘后自动关闭，并拒绝重复启动同一登录会话。
- 作品总账和检查点作为可重建索引，不再因索引同步异常反向破坏已落盘的抓取或转写结果；页面在总账接口暂不可用时回退旧统计。
- 博主智能体材料索引改为读取完整转写历史并按作品 ID 去重，不再因最近 500 条查询上限遗漏其他博主。
- “关注与更新”改为读取完整转写索引并按作品 ID 去重统计，不再因最近 500 条查询上限显示错误数量；博主详情同步展示运行中或可恢复批次，并可直接打开原始 JSON 与全部转写位置。
- 未完成的全量转写批次会在工作台优先显示真实总数、已完成数和剩余数，并提供“继续剩余作品”入口；继续时只重试失败项。
- 混合转写批次的文件入口会打开该批次涉及的全部通道目录，不再只展示云端或本地其中一个目录。
- 修复同一服务进程遗留 Chrome Profile 锁导致本地 Whisper 大批量失败的问题；Cookie 导出现在串行等待 Profile，真实并发仍受锁保护。
- 批量转写时底部操作浮层保持紧凑高度，长状态说明最多显示两行，操作按钮不再逐字竖排。
- 全量快照继续严格校验主页数量；追加式增量允许历史累计资产与当前主页计数不同，同时对每条新增作品执行归属和必填字段门禁。
- 页面不再用云端 100 条参考额度限制整个混合批次；每条作品保留实际执行通道。
- 增量目录恢复会合并上一次审核通过的基线，并隔离无法证明属于目标博主的 DOM 噪声卡片。
- 修正订阅成功时提前覆盖旧基线，导致新增作品数始终显示为 0 的问题。
- 升级 Axios、Playwright，并锁定已修复的传递依赖版本，清理 GitHub Dependabot 安全告警。

## [0.1.0] - 2026-07-28

### Added

- 博主主页、单条作品和本人收藏夹目录获取。
- JSON 证据审核、去重和有限恢复 Loop。
- 云端链接转写与本地 FFmpeg + Whisper 双通道。
- SQLite 任务、日志、订阅、缓存、审计结论和产物索引。
- 多作品爆款拆解、证据选题、博主智能体画像与稿件审阅。
- 内容采集账号和收藏夹账号共享或独立登录。

### Known limitations

- 当前主要验证环境为 Windows 单用户本地运行。
- 收藏夹全目录数量对账仍需更多账号样本。
- 页面结构和平台策略变化可能导致目录抓取需要适配。
