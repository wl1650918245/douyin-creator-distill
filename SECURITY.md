# 安全政策

## 支持范围

当前仅维护最新的 `main` 分支和最新 Release。CreatorDistill 是单用户本地产品，不提供
云端账号、远程控制或公网服务。

## 报告安全问题

请通过 GitHub 仓库的 **Security > Report a vulnerability** 私密报告以下问题：

- API 密钥、Cookie、Chrome Profile 或本机路径泄露；
- 本地接口允许读取知识资产根目录之外的文件；
- 路径穿越、任意命令执行或不安全的文件覆盖；
- 页面返回配置文件中的敏感字段；
- 第三方依赖存在可被当前产品实际触发的高危漏洞。

不要在公开 Issue 中粘贴真实 Cookie、API 密钥、账号 ID、收藏夹内容、日志或本机路径。

报告中请包含受影响版本、复现步骤、预期结果、实际结果和最小脱敏证据。维护者确认前，
请勿公开漏洞细节。

## 本地安全边界

- `config/*.config.json`、`.env`、SQLite、日志、模型和 Chrome Profile 默认被 Git 忽略。
- 本地服务默认只监听 `127.0.0.1`，不要直接暴露到公网。
- 页面不会返回 API 密钥和 Chrome Profile 实际路径。
- 登录失效、验证码、`403` 和 `429` 会停止自动请求，不会绕过平台验证。
- 第三方模型和转写服务会收到用户明确提交的内容，使用前请自行评估其隐私政策。

## 依赖更新

安全更新应先完成最小回归，再升级锁文件。不要在没有验证真实运行路径的情况下批量更新
抓取、浏览器自动化、数据库或模型依赖。
